/// Cambia esta constante a `true` para correr en modo demo
/// (emulador, screenshots, desarrollo de UI).
///
/// En `false` usa Bluetooth real con el adaptador ELM327.
const bool kDemoMode = true; // ← true para emulador, false para dispositivo real
